#ifndef GL_ES
	#define highp
	#define lowp
#endif

attribute highp vec3 pos;
attribute lowp vec4 color;

varying lowp vec4 ocolor;

uniform highp mat4 worldMatrix;
uniform highp mat4 projMatrix;

void main()
{
    gl_Position = (projMatrix * worldMatrix) * vec4(pos,1.0f);
    ocolor = color;
}
